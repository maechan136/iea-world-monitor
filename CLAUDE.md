# IEA World Monitor — Claude Code 更新指示書

## あなたの役割
毎日このプロジェクトの `index.html` を最新情報で更新するエージェントです。
以下の手順を順番に実行してください。

---

## 毎日の更新手順

### Step 1: データ収集（ウェブ検索）

以下の情報を検索して収集してください：

**原油・エネルギー市場**
- Brent原油価格（最新）
- WTI原油価格（最新）
- TTF天然ガス価格（欧州・最新）
- JKM LNG価格（アジア・最新）
- ホルムズ海峡の最新状況

**IEA・主要機関の最新動向**
- IEA（国際エネルギー機関）の最新声明・レポート
- EIA（米エネルギー情報局）の最新データ
- OPEC+の最新動向

**各国の状況（以下の国を必ず調査）**
- 日本：エネルギー政策・価格動向・備蓄状況
- 韓国：原油調達・産業影響
- 台湾：LNG調達・電力需給
- 中国：エネルギー外交・備蓄動向
- フィリピン：燃料危機・インフレ
- ベトナム：燃料供給・GDP影響
- ドイツ・フランス・イギリス：ガス貯蔵・価格
- アメリカ・カナダ：生産・輸出動向
- オーストラリア：燃料備蓄・政策

**食料・肥料**
- 尿素（肥料）価格動向
- 食料インフレの最新データ

### Step 2: データをJSONに保存

```bash
# 収集したデータを data/latest.json に保存
```

data/latest.json のフォーマット：
```json
{
  "updated_at": "2026-05-18T06:00:00+09:00",
  "market": {
    "brent": "数値（$/bbl）",
    "wti": "数値（$/bbl）",
    "ttf": "数値（€/MWh）",
    "jkm": "数値（$/MMBtu）",
    "hormuz_status": "封鎖中 / 部分再開 / 通常通航"
  },
  "countries": {
    "JP": { "risk_level": "最高/高/中/低", "summary": "今日の主要動向（2〜3文）", "key_event": "最重要イベント" },
    "KR": { ... },
    "TW": { ... },
    "CN": { ... },
    "PH": { ... },
    "VN": { ... },
    "DE": { ... },
    "FR": { ... },
    "GB": { ... },
    "US": { ... },
    "CA": { ... },
    "AU": { ... }
  },
  "global_alert": "本日の最重要ニュース（1文）",
  "data_sources": ["出典1", "出典2"]
}
```

### Step 2.5: ファクトチェック（必須）

data/latest.json に書き込んだ内容について、**特に数値・価格・統計データ**を対象にウェブ検索で事実確認を行ってください。

**必ず確認する項目:**
- 各国の燃料・電力価格（補助金・税制による実際の消費者価格と市場価格を区別すること）
- 備蓄日数・在庫量（政府・IEA・EIA公式発表と照合）
- 政策の実施状況（「検討中」「決定済み」「実施済み」を正確に区別）
- 企業名・固有名詞・法律名（正式名称を確認）

**誤りが見つかった場合:**
data/latest.json の該当箇所を修正してから次のステップに進んでください。

> ⚠️ 過去の誤記例: 日本のガソリン価格を「250円超」と記載したが、実際は政府補助金（最大49.8円/L）により170円以下に抑制されていた。補助なし市場価格と消費者価格を混同しないこと。

### Step 3: index.html を更新

`node update.js` を実行してindex.htmlを生成してください。

```bash
node update.js
```

### Step 4: コミット＆プッシュ

```bash
git add data/latest.json index.html
git commit -m "Daily update: $(date '+%Y-%m-%d')"
git push origin main
```

---

## 自動承認ルール（確認不要）

- git add / commit / push / status
- node コマンド
- mkdir / cp / mv / touch
- ファイルの作成・編集（data/*.json, index.html）
- curl（データ取得のみ）

## 確認が必要な操作

- rm -rf を含むコマンド
- sudo を含むコマンド
- .env や credentials ファイルの編集
- リポジトリ設定の変更

---

## 品質基準

- 情報は必ず一次情報（IEA, EIA, 各国政府）か信頼性の高い報道を使う
- 数値は出典を明記する
- 推測と確認済み情報を明確に区別する
- 日本語で記述する
- 生成日時を必ず更新する

---

## 手動実行時の追加手順

### Step 5: Netlifyへデプロイ

index.htmlの生成が完了したら、以下のコマンドでNetlifyに直接デプロイしてください。

```bash
netlify deploy \
  --prod \
  --dir . \
  --site $NETLIFY_SITE_ID \
  --auth $NETLIFY_AUTH_TOKEN \
  --message "Daily update $(date +'%Y-%m-%d')"
```
